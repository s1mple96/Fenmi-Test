# ETC申办系统依赖包
# 仅保留项目实际用到的第三方库（不要列出标准库）

# 数据库
pymysql>=1.0.2

# 网络和HTTP
requests>=2.25.0
urllib3>=1.26.0
certifi>=2020.12.0
charset-normalizer>=2.0.0
idna>=2.10

# HTML解析（VIN获取功能需要）
beautifulsoup4>=4.9.0

# GUI 框架
PyQt5>=5.15.0

# 打包工具（可选）
# nuitka>=1.0.0

# 以下依赖ETC申办系统不直接使用，但被某些工具类间接依赖：
# paramiko>=2.7.0  # 仅test/和linux_util用到，ETC申办系统不需要
# cryptography>=3.4.0  # paramiko的依赖
# bcrypt>=3.2.0  # paramiko的依赖
# asn1crypto>=1.4.0  # paramiko的依赖 