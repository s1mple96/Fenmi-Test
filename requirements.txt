# ETC申办系统依赖包
# 仅保留项目实际用到的第三方库（不要列出标准库）

# 数据库
pymysql>=1.0.2

# 网络和HTTP
requests>=2.25.0
urllib3>=1.26.0
certifi>=2020.12.0
charset-normalizer>=2.0.0
idna>=2.10

# SSH 与加密（被 linux_util/test 用到）
paramiko>=2.7.0
cryptography>=3.4.0
bcrypt>=3.2.0
asn1crypto>=1.4.0

# GUI 框架
PyQt5>=5.15.0

# 打包工具（可选）
# nuitka>=1.0.0 